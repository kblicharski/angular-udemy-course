import { BestHighlightDirective } from './best-highlight.directive';

describe('BestHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BestHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
