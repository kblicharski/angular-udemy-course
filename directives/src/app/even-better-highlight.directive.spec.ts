import { EvenBetterDirective } from './even-better-highlight.directive';

describe('EvenBetterDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenBetterDirective();
    expect(directive).toBeTruthy();
  });
});
