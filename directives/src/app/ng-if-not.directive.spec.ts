import { NgIfNotDirective } from './ng-if-not.directive';

describe('NgIfNotDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfNotDirective();
    expect(directive).toBeTruthy();
  });
});
